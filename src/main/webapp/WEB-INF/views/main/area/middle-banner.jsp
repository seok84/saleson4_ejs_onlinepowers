<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt" %>

<section class="main-ad-banner-area">
    <div class="flex">

        <%--<c:forEach var="middleBanner" items="${promotion.middleBanner}">
            <c:if test="${not empty middleBanner.image}">
                <a href="${middleBanner.url}" class="img-banner pc">
                    <img src="${middleBanner.image}" alt="${middleBanner.content}" alt="" onerror="errorImage(this)" loading="lazy" decoding="async">
                </a>
            </c:if>
        </c:forEach>

        <c:forEach var="middleBanner" items="${promotion.mobileMiddleBanner}">
            <c:if test="${not empty middleBanner.image}">
                <a href="${middleBanner.url}" class="img-banner mobile">
                    <img src="${middleBanner.image}" alt="${middleBanner.content}" class="mobile" onerror="errorImage(this)" loading="lazy" decoding="async">
                </a>
            </c:if>
        </c:forEach>--%>
    </div>

</section>